{
  "permissions": {
    "allow": [
      "Bash(git init *)",
      "Bash(git add *)",
      "Bash(git -c commit.gpgsign=false commit -q -m 'Add Listahan v1 design spec *)",
      "Bash(git -c commit.gpgsign=false commit -q -m 'Re-tune screen directions to accessibility-first *)",
      "Bash(git -c commit.gpgsign=false commit -q -m 'Add iconography decision: replace emoji with a real vector icon set *)",
      "Bash(git -c commit.gpgsign=false commit -q -m 'Add Plan 1: foundation & data layer implementation plan *)"
    ]
  }
}
